# Gitleaks configuration for OttieVerse pre-commit secret scanning.
# Reference: https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[allowlist]
description = "Project-level allowlist"
paths = [
  '''pnpm-lock\.yaml$''',
  '''\.gitleaks\.toml$''',
]
